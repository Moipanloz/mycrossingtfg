dist: trusty
sudo: false

language: node_js
node_js:
  - '8'

addons:
  chrome: stable

cache:
  directories:
    - ./node_modules

install:
  - npm install
  - npm update

script:
  - npm run test -- --watch=false --no-progress --browsers=ChromeHeadlessNoSandbox