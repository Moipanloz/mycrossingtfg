import { FiltroRopaPipe } from './filtro-ropa.pipe';

describe('FiltroRopaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroRopaPipe();
    expect(pipe).toBeTruthy();
  });
});
