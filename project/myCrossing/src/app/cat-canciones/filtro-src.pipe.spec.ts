import { FiltroSrcPipe } from './filtro-src.pipe';

describe('FiltroSrcPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroSrcPipe();
    expect(pipe).toBeTruthy();
  });
});
