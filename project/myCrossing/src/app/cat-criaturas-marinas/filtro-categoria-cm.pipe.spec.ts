import { FiltroCategoriaCmPipe } from './filtro-categoria-cm.pipe';

describe('FiltroCategoriaCmPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCategoriaCmPipe();
    expect(pipe).toBeTruthy();
  });
});
