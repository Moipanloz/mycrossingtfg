import { TraducirCmPipe } from './traducir-cm.pipe';

describe('TraducirCmPipe', () => {
  it('create an instance', () => {
    const pipe = new TraducirCmPipe();
    expect(pipe).toBeTruthy();
  });
});
