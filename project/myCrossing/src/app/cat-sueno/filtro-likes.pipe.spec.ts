import { FiltroLikesPipe } from './filtro-likes.pipe';

describe('FiltroLikesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroLikesPipe();
    expect(pipe).toBeTruthy();
  });
});
