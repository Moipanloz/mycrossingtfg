import { FiltroSuenosPipe } from './filtro-suenos.pipe';

describe('FiltroSuenosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroSuenosPipe();
    expect(pipe).toBeTruthy();
  });
});
