import { TraducirPezPipe } from './traducir-pez.pipe';

describe('TraducirPezPipe', () => {
  it('create an instance', () => {
    const pipe = new TraducirPezPipe();
    expect(pipe).toBeTruthy();
  });
});
