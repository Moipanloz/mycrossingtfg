import { FiltroCategoriaPezPipe } from './filtro-categoria-pez.pipe';

describe('FiltroCategoriaPezPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCategoriaPezPipe();
    expect(pipe).toBeTruthy();
  });
});
