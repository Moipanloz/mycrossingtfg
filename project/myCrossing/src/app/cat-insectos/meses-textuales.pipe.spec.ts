import { MesesTextualesPipe } from './meses-textuales.pipe';

describe('MesesTextualesPipe', () => {
  it('create an instance', () => {
    const pipe = new MesesTextualesPipe();
    expect(pipe).toBeTruthy();
  });
});
