import { FiltroVecinoPipe } from './filtro-vecino.pipe';

describe('FiltroVecinoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroVecinoPipe();
    expect(pipe).toBeTruthy();
  });
});
